import React, {Fragment} from 'react';

const Membre = ({nom, famille }) => {
    return (
        <Fragment>
            <h1>Membre : {nom}</h1>
            {/* <p>{chi²ldren}</p> */}
        </Fragment>
    )
}

export default Membre